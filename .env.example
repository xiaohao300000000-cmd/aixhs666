# Existing Miaoda platform variables are injected by the runtime.

# Stable public URL of the existing FastAPI service, without a trailing slash.
OPERATOR_API_BASE_URL=https://api.example.com

# Server-only token matching OPS_TOKEN on the FastAPI service.
OPERATOR_API_TOKEN=replace-with-server-only-token
