# 子任务报告：T12

## 执行结果

- 状态：COMPLETED
- 分支：task/T12-comment-dynamic-budget
- 提交：本报告所在提交
- 执行会话：T12 独立子 CLI

## 完成内容

- 新增 `intelligence.comment_budget` 纯逻辑模块，支持批次指标、预算状态、继续/停止/强制上限决策。
- 在评论采集 worker 中接入动态预算，可从 `payload_json.dynamic_budget` 或 cursor 中启用，并把预算状态写回 `cursor_json`。
- 新增 T12 测试覆盖指标计算、连续低新增停止、高价值评论区放宽、强制上限和 worker 默认批次/剩余额度边界。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `intelligence/comment_budget/__init__.py` | 导出评论动态预算 API |
| `intelligence/comment_budget/budget.py` | 实现批次统计、状态序列化和预算决策 |
| `apps/worker/comment_collection.py` | 接入 worker 动态预算入口和 cursor 状态持久化 |
| `tests/test_comment_dynamic_budget.py` | 增加 T12 验收测试 |
| `orchestration/reports/T12.md` | 创建子任务报告 |

## 测试结果

```text
.venv/bin/pytest tests/test_comment_dynamic_budget.py
7 passed in 0.17s

.venv/bin/pytest
74 passed, 1 warning in 0.51s
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 能计算每批新增用户率、新表达率和重复率 | 是 | `tests/test_comment_dynamic_budget.py::test_comment_batch_metrics_count_newness_duplicates_regions_and_institutions` |
| 连续两批低新增时停止 | 是 | `tests/test_comment_dynamic_budget.py::test_budget_stops_after_two_consecutive_low_information_batches` |
| 高价值评论区可以继续展开 | 是 | `tests/test_comment_dynamic_budget.py::test_high_value_comment_section_relaxes_continue_threshold` |
| 强制上限生效 | 是 | `tests/test_comment_dynamic_budget.py::test_budget_enforces_parent_and_total_comment_limits` 和 worker 边界测试 |
| 不修改数据库模型、迁移、调度状态机和全局进度文件 | 是 | 修改范围仅限简报允许文件 |
| `pytest` 通过 | 是 | 全量 `74 passed, 1 warning` |

## 未完成事项

- 无。

## 风险与问题

- 新表达、地域和机构识别为第一版规则启发式，后续 T14/T15 文本处理与新词发现完成后可替换为更稳健的抽取逻辑。

## 决策建议

- 不需要主控决策。

## 建议的下一步

- 主控验收 T12 后，可继续验收/派发依赖评论区产出指标的后续任务。
