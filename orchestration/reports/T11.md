# 子任务报告：T11

## 执行结果

- 状态：COMPLETED
- 分支：task/T11-high-value-source-pool
- 提交：本报告所在提交
- 执行会话：T11 独立子 CLI

## 完成内容

- 新增高价值来源池数据对象与内存 upsert 服务，支持帖子、账号、评论区、种子账号、竞品账号。
- 新增分数驱动的 tracking_interval/next_check_at 计算，高分来源更高频检查。
- 新增 worker 层入口，可从已有内容与评论上下文生成可解释候选来源。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `intelligence/source_pool/__init__.py` | 导出高价值来源池公共接口 |
| `intelligence/source_pool/pool.py` | 实现来源类型、候选对象、来源对象、幂等内存池、追踪频率计算和候选构造函数 |
| `apps/worker/source_pool.py` | 提供 worker 可调用入口，从内容/评论信号生成来源候选 |
| `tests/test_high_value_source_pool.py` | 覆盖 T11 来源创建、幂等 upsert、频率计算和 worker 入口 |
| `orchestration/reports/T11.md` | 创建子任务报告 |

## 测试结果

```text
.venv/bin/pytest tests/test_high_value_source_pool.py
5 passed in 0.01s

.venv/bin/pytest
61 passed, 1 warning in 6.08s
```

说明：直接运行 `pytest` 失败，因为系统 shell 没有全局 pytest；直接运行 `python3 -m pytest` 失败，因为系统 `python3` 是 3.9 且未安装 pytest。已使用本机 Python 3.12 创建 `.venv` 并安装 `.[dev]` 后完成测试。

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 可创建高价值帖子、账号、评论区、种子账号、竞品账号来源 | 是 | `test_source_pool_creates_all_required_source_types` |
| 相同 source_type/platform/source_id 重复加入时幂等更新，不重复 | 是 | `test_upsert_is_idempotent_and_merges_score_reason_and_metadata` |
| 每个来源有可解释 reason、score 和 metadata | 是 | 来源对象强制 reason/score，worker 候选写入 reason_signals metadata |
| tracking_interval/next_check_at 可根据 score 计算 | 是 | `test_tracking_interval_and_next_check_are_score_based` |
| worker 入口可从已有内容和评论信号生成候选来源 | 是 | `test_worker_builds_candidates_from_content_and_comment_signals`、`test_worker_generates_commenter_accounts_for_high_value_commenters` |
| 不修改数据库模型、迁移、调度状态机和全局进度文件 | 是 | 修改范围仅限 T11 允许文件 |
| pytest 通过 | 是 | 全量 `61 passed, 1 warning` |

## 未完成事项

- 未接入真实数据库持久化和真实平台增量检查，符合 T11 简报范围。

## 风险与问题

- 关键词信号为第一版本地规则，后续 T13/T14 可用真实数据校准权重与信号命名。

## 决策建议

- 不需要主控新增决策。

## 建议的下一步

- 主控验收 T11 后，可派发 T12 评论区动态预算或 T13 查询与来源评分。
