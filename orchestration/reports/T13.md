# 子任务报告：T13

## 执行结果

- 状态：COMPLETED
- 分支：`task/T13-query-source-scoring`
- 提交：见 Git 提交记录
- 执行会话：T13 独立子 CLI

## 完成内容

- 新增查询/来源统计输入对象 `QuerySourceStats`。
- 新增评分结果对象 `QuerySourceScore`，输出新增率、新用户率、新表达率、重复率、失败率、任务价值分和解释原因。
- 新增 `score_query_source` 和 `rank_query_sources`，按架构公式计算并排序候选查询/来源。
- 增加 T13 单元测试，覆盖费率计算、正向增益、重复/失败惩罚、排序和无样本边界。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `intelligence/scoring/__init__.py` | 导出 T13 评分对象和函数 |
| `intelligence/scoring/query_source.py` | 实现查询与来源评分逻辑 |
| `tests/test_query_source_scoring.py` | 增加 T13 验收测试 |
| `orchestration/reports/T13.md` | 创建子任务报告 |

## 测试结果

```text
.venv/bin/pytest tests/test_query_source_scoring.py
6 passed in 0.01s

.venv/bin/pytest
73 passed, 1 warning in 5.68s
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 能计算新增率、新用户率、新表达率、重复率、失败率 | 是 | `tests/test_query_source_scoring.py::test_score_calculates_rates_and_explainable_task_value` |
| 能输出可解释任务价值分 | 是 | `QuerySourceScore.reason` 包含公式、费率和归一化值 |
| 高失败率和高重复率会降低分数 | 是 | `test_high_duplicate_and_failure_rates_reduce_score` |
| 高新增、高新用户、高新表达会提高分数 | 是 | `test_high_new_content_user_and_expression_rates_raise_score` |
| 可对候选查询/来源排序 | 是 | `rank_query_sources` 和 `test_rank_query_sources_orders_by_value_with_penalty_tiebreakers` |
| 不修改数据库模型、迁移、查询 API、调度状态机和全局进度文件 | 是 | 修改范围仅限 T13 允许文件 |
| `pytest` 通过 | 是 | 全量 `73 passed, 1 warning` |

## 未完成事项

- 无。

## 风险与问题

- 当前评分输入由调用方提供统计值，本任务未接入数据库聚合或调度器。
- `collection_cost`、`coverage_gap_value`、`context_completion_value` 采用 0 到 1 的归一化输入，后续接入真实统计时需保持同一尺度。

## 决策建议

- 暂无需要主控决策的事项。

## 建议的下一步

- 主控验收后，可在 T17/T20 中复用 `rank_query_sources` 作为事件频率调整和看板来源评分的基础。
