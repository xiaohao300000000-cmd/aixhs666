# 子任务报告：T08

## 执行结果

- 状态：COMPLETED
- 分支：task/T08-xhs-detail-collection
- 提交：当前提交（见最终输出）
- 执行会话：独立执行子 CLI

## 完成内容

- 新增小红书详情采集 worker 入口，支持领取 `collect_content` 任务并通过 `target_id` 或 `payload_json.platform_content_id` 获取详情。
- 详情采集写入内容 L1 字段和作者公开资料，复用现有幂等入库函数。
- 详情采集保存 `content_detail` 原始快照，快照包含内容、标签、图片链接和作者资料。
- Mock 采集器补充标签和图片链接，用于覆盖 L1 快照验收。
- 新增 T08 详情采集测试，覆盖成功采集、payload 目标、幂等、失败重试和缺失目标校验。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `apps/worker/detail_collection.py` | 新增详情采集任务执行逻辑 |
| `collectors/base.py` | 为统一内容对象补充通用 L1 字段 `tags`、`image_urls` |
| `collectors/mock.py` | 补充 Mock 详情数据中的标签和图片链接 |
| `tests/test_xhs_detail_collection.py` | 新增详情采集专项测试 |
| `orchestration/reports/T08.md` | 创建子任务报告 |

## 测试结果

```text
$ /tmp/ai-hawke-xhs-t08-venv/bin/pytest tests/test_xhs_detail_collection.py tests/test_xhs_search_collection.py tests/test_storage_ingest.py tests/test_mock_platform_adapter.py
24 passed in 5.98s

$ /tmp/ai-hawke-xhs-t08-venv/bin/pytest
45 passed, 1 warning in 0.61s
```

说明：系统环境无 `pytest` 且默认 `python3` 为 3.9.6；项目要求 Python 3.12。已使用 `/tmp` 临时 venv 和 Python 3.12.13 运行测试，未提交环境文件或依赖清单变更。

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 正文、作者、时间、互动数据写入 L1 | 是 | `tests/test_xhs_detail_collection.py::test_detail_task_collects_l1_fields_saves_snapshot_and_completes` |
| 标签进入详情采集结果 | 是 | `CollectedContent.tags` 并写入 `content_detail` 快照 |
| 原始快照可按路径查回 | 是 | 测试读取 `Snapshot.object_storage_path` 并校验内容 |
| 重复详情采集幂等 | 是 | `test_repeated_detail_execution_is_idempotent_for_content_and_profile` |
| 采集失败进入重试并记录错误 | 是 | `test_detail_adapter_error_moves_task_to_retry_and_records_error` |
| 不修改数据库模型、迁移、调度状态机、查询 API、全局进度文件或 docs | 是 | 仅修改允许范围内文件 |

## 未完成事项

- 未接入真实小红书 Chrome 页面解析；本任务完成统一 adapter 边界和 worker 入库闭环。
- 当前数据模型无标签和图片链接列，标签和图片链接保存在原始详情快照中。

## 风险与问题

- `orchestration/briefs/T08.md` 在仓库中不存在；本次按用户点名文件、`TASKS.md`、架构 L1 描述和验收标准执行。
- 真实 Chrome 登录态未验证。

## 决策建议

- 如需查询/导出标签和图片链接，主控需决定后续是否扩展数据模型和迁移；本子任务未修改模型。
- 主控需决定真实小红书页面解析器和 Chrome 登录态验证任务是否单独派发。

## 建议的下一步

- 主控验收 T08 后，可派发 T09 小红书评论采集。
