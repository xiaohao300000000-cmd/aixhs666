# 子任务报告：T16

## 执行结果

- 状态：COMPLETED
- 分支：task/T16-feishu-phrase-review
- 提交：本报告所在提交
- 执行会话：Codex T16 独立子 CLI

## 完成内容

- 新增飞书新词审核 payload 构造函数，仅生成本地可测试对象，不调用真实网络。
- 新增审核动作与状态转换对象，支持 approve、reject、convert_to_query，并对非法转换抛出错误。
- 新增 approved/converted 审核对象转查询创建请求的纯函数。
- 新增 worker 可调用入口，接受候选表达列表并返回待推送 payload。
- 新增 T16 专项测试覆盖 payload、状态转换、非法转换、转查询和 worker 入口。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `integrations/feishu/__init__.py` | 暴露飞书新词审核集成边界 |
| `integrations/feishu/phrase_review.py` | 实现 payload、审核状态、动作转换和转查询请求纯函数 |
| `apps/worker/phrase_review.py` | 提供 worker 可调用的候选表达转审核 payload 入口 |
| `tests/test_feishu_phrase_review.py` | 覆盖 T16 验收条件 |
| `orchestration/reports/T16.md` | 记录子任务执行结果 |

## 测试结果

```text
$ pytest
zsh:1: command not found: pytest

$ python -m pytest
zsh:1: command not found: python

$ python3 -m pytest
/Library/Developer/CommandLineTools/usr/bin/python3: No module named pytest

$ ../T15/.venv/bin/python -m pytest
collected 77 items
77 passed, 1 warning in 0.57s
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 可为候选词构造飞书审核 payload | 是 | `build_phrase_review_payload` 和 `test_build_phrase_review_payload_contains_card_actions_without_network_io` |
| 可批准、拒绝候选词 | 是 | `apply_phrase_review_action` 和 `test_phrase_review_actions_approve_reject_and_convert_statuses` |
| 批准候选可转换为查询请求 | 是 | `phrase_review_to_query_request` 和 `test_approved_or_converted_review_can_create_query_request` |
| 状态转换可测试且非法转换报错 | 是 | `test_illegal_phrase_review_transition_raises` |
| 不调用真实飞书网络、不新增依赖、不修改数据库模型/迁移/全局进度文件 | 是 | 仅新增 `integrations/feishu/**`、`apps/worker/phrase_review.py`、测试和本报告 |
| `pytest` 通过 | 是 | 使用相邻 worktree 既有虚拟环境执行：`77 passed, 1 warning` |

## 未完成事项

- 无。

## 风险与问题

- T16 worktree 未自带 `.venv`，本机 `pytest`、`python` 命令不可用；测试通过复用相邻 T15 worktree 的虚拟环境执行。
- 当前实现只构造飞书交互卡片 payload，不包含真实发送、签名校验或数据库回写。

## 决策建议

- 不需要主控决策。

## 建议的下一步

- 主控验收后再决定是否在后续任务中接入真实飞书发送与审核回调持久化。
