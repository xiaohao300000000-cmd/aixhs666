# 子任务报告：T10

## 执行结果

- 状态：COMPLETED
- 分支：`task/T10-resume-partial-success`
- 提交：最终提交哈希见子会话最终输出
- 执行会话：W1

## 完成内容

- 在 worker 层新增显式 partial 任务续跑入口，不修改 `scheduler/**`。
- 搜索任务可从 `cursor_json.next_cursor` 继续采集下一页，并复用既有幂等入库逻辑。
- 评论任务可从 `cursor_json.next_cursor` 继续采集下一页，并保持评论与回复关系幂等。
- 覆盖单页已入库后异常、保留旧游标、进入 retry、重试后最终完成的场景。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `apps/worker/resume.py` | 新增 partial 任务恢复为 running 的 worker 辅助入口 |
| `apps/worker/search_collection.py` | 暴露 `resume_partial_search_task`，复用现有搜索采集逻辑 |
| `apps/worker/comment_collection.py` | 暴露 `resume_partial_comment_task`，复用现有评论采集逻辑 |
| `tests/test_resume_partial_success.py` | 覆盖搜索恢复、评论恢复、异常保留游标、重试幂等和最终完成 |
| `orchestration/reports/T10.md` | T10 子任务报告 |

## 测试结果

```text
$ pytest tests/test_resume_partial_success.py -q
zsh:1: command not found: pytest

$ /Users/xiaohao30000/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3 -m venv /tmp/ai-hawke-xhs-t10-venv
$ /tmp/ai-hawke-xhs-t10-venv/bin/python -m pip install -e '.[dev]'
Successfully installed ai-hawke-xhs-0.1.0 ... pytest-8.4.2 ...

$ /tmp/ai-hawke-xhs-t10-venv/bin/python -m pytest tests/test_resume_partial_success.py -q
4 passed in 5.85s

$ /tmp/ai-hawke-xhs-t10-venv/bin/python -m pytest
collected 56 items
56 passed, 1 warning in 0.53s
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 搜索任务可从已有搜索游标继续，不重复第一页 | 是 | `test_resume_partial_search_task_continues_from_saved_cursor` 断言恢复调用 cursor 为 `"1"`，不是 `None` |
| 评论任务可从已有评论游标继续，不重复第一页 | 是 | `test_resume_partial_comment_task_continues_from_saved_cursor` 断言恢复调用 cursor 为 `"1"`，不是 `None` |
| Worker 中断或异常后，已成功入库的数据保留 | 是 | `test_search_partial_success_failure_keeps_cursor_and_retry_is_idempotent` 在快照失败后仍保留第二页内容 |
| 重试继续时不会重复创建内容、评论、发现关系或回复关系 | 是 | 搜索重试后内容和发现关系数量保持 2；评论重复执行后评论数量保持 2 且回复关系正确 |
| 有后续页时任务保留 `partial` 和最新游标 | 是 | 现有 T07/T09 测试和 T10 首段运行均断言 `partial` 与 `next_cursor` |
| 无后续页时任务进入 `completed` | 是 | 搜索和评论恢复最终都断言 `completed` |
| 异常时通过状态机进入 retry/failed，并记录错误 | 是 | 搜索快照失败和评论 adapter 失败均进入 `retry` 并保留错误 |
| 测试覆盖搜索恢复、评论恢复、部分成功后异常、重试幂等和最终完成 | 是 | `tests/test_resume_partial_success.py` 四个场景覆盖 |
| 不修改数据库模型、迁移、查询 API、调度状态机和全局进度文件 | 是 | 修改范围仅限允许的 worker、测试和报告文件 |

## 未完成事项

- 无。

## 风险与问题

- partial 任务仍不会被 `scheduler.claim_next_task` 自动领取；本任务按简报约束在 worker 层提供显式 `resume_partial_*_task` 入口。
- 全量测试存在一个 FastAPI/TestClient 上游弃用警告，不影响本任务功能。

## 决策建议

- 无需要主控决定的事项。

## 建议的下一步

- 主控验收 T10 分支代码、测试结果和报告后，再决定是否在后续任务中把 partial 续跑入口接入实际 worker 调度循环。
