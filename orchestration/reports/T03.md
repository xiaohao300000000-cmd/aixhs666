# 子任务报告：T03

## 执行结果

- 状态：COMPLETED
- 分支：task/T03-task-state-machine
- 提交：未提交；当前 HEAD 为 ef41f41，子会话按要求等待主控验收，不自行提交或合并
- 执行会话：W1

## 完成内容

- 新增 `scheduler.task_state_machine`，实现 collection task 创建、领取、完成、partial、取消、失败重试、手动重试与超时恢复。
- `claim_next_task` 只领取 `pending` / `retry` 且已到 `scheduled_at` 的任务，并写入 `worker_id`、`started_at`、`status=running`。
- 新增状态转换测试，覆盖可运行任务筛选、完成、失败重试上限、超时恢复、partial、cancel，以及失败任务不影响其他任务。
- 按主控验收意见补充 `schedule_retry` 和终态保护测试：手动重试会清理运行痕迹并设置重试时间/错误；终态任务不能重试；已完成任务不能取消。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `scheduler/__init__.py` | 导出 T03 状态机公开接口 |
| `scheduler/task_state_machine.py` | 新增 collection task 状态机核心逻辑 |
| `tests/test_task_state_machine.py` | 新增状态转换和调度行为测试 |
| `orchestration/reports/T03.md` | 创建本子任务执行报告 |

## 测试结果

```text
pytest
结果：失败，系统 PATH 中无 pytest：zsh:1: command not found: pytest

python3 -m pytest
结果：失败，系统 Python 3.9.6 无 pytest：No module named pytest

/tmp/t03-task-state-machine-venv/bin/python -m pytest
结果：通过。
环境：使用 Codex bundled Python 3.12.13 创建的临时 venv，安装 pytest、sqlalchemy、fastapi、httpx、psycopg[binary]。
摘要：16 passed, 1 warning in 0.39s。
警告：FastAPI/TestClient 依赖链中的 StarletteDeprecationWarning，与本任务状态机逻辑无关。
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 状态转换有测试覆盖 | 是 | `tests/test_task_state_machine.py` 覆盖 claim、complete、fail/retry、manual schedule_retry、timeout recover、partial、cancel |
| Worker 领取任务时只领取可运行任务，并写入 `worker_id`、`started_at`、`status=running` | 是 | `test_claim_next_task_only_claims_runnable_tasks` |
| 失败任务在未超过 `max_attempts` 时进入可重试状态，超过后进入 `failed` | 是 | `test_fail_task_retries_until_max_attempts_without_affecting_other_tasks` |
| 超时 `running` 任务可恢复为 `retry` 或 `pending`，并保留错误信息 | 是 | `test_recover_timed_out_tasks_restores_only_stale_running_tasks`、`test_recover_timed_out_tasks_can_restore_to_pending` |
| `schedule_retry` 能将非终态任务转为 `retry` 并清理运行字段 | 是 | `test_schedule_retry_resets_non_terminal_task_for_future_claim` |
| 终态任务不能被 `schedule_retry` | 是 | `test_schedule_retry_rejects_terminal_tasks` 覆盖 `completed` 和 `cancelled` |
| 已完成任务不能再次 `cancel_task` | 是 | `test_cancel_task_rejects_completed_task` |
| `partial` 和 `cancelled` 状态可被明确设置 | 是 | `test_mark_partial_and_cancel_set_explicit_statuses` |
| 一个任务失败不影响其他任务 | 是 | `test_fail_task_retries_until_max_attempts_without_affecting_other_tasks` 中断言其他任务仍为 `pending` |
| 不修改数据库模型和迁移 | 是 | 仅修改 `scheduler/**`、`tests/**` 和 `orchestration/reports/T03.md` |

## 未完成事项

- 无。

## 风险与问题

- 本机默认命令行没有可直接使用的 `pytest`，系统 Python 也不是项目要求的 3.12；已使用临时 Python 3.12 venv 完成验证。
- 测试环境存在第三方 `StarletteDeprecationWarning`，不影响当前测试通过。

## 决策建议

- 无需主控新增产品或数据模型决策。

## 建议的下一步

- 主控验收 T03 变更后，再决定是否提交、合并或派发后续任务。
