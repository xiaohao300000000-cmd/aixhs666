# 子任务报告：T07

## 执行结果

- 状态：COMPLETED
- 分支：task/T07-xhs-search-collection
- 提交：随 `feat(T07): add xhs search collection` 提交；最终提交哈希见主控回复
- 执行会话：W1R

## 完成内容

- 新增 worker 层搜索采集入口，支持领取并执行 `search` 类型任务。
- 搜索结果通过 `ingest_search_results` 写入 L0 `contents` 与 `discovery_relations`。
- 搜索页原始响应保存为稳定 JSON 快照，并写入 `Snapshot(entity_type="query", snapshot_type="search_page")`。
- 分页游标写入 `CollectionTask.cursor_json`，无后续页完成任务，有后续页标记 partial。
- Adapter 或入库链路异常时通过既有状态机 `fail_task` 进入 retry/failed，并记录错误。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `apps/worker/search_collection.py` | 新增搜索任务领取和执行入口 |
| `storage/snapshots.py` | 新增稳定 JSON 快照保存与 Snapshot 记录写入 |
| `storage/__init__.py` | 导出快照保存 helper |
| `tests/test_xhs_search_collection.py` | 覆盖 T07 搜索采集验收路径 |
| `orchestration/reports/T07.md` | 子任务执行报告 |

## 测试结果

```text
pytest
zsh:1: command not found: pytest

python -m pytest
zsh:1: command not found: python

python3 -m pytest
/Library/Developer/CommandLineTools/usr/bin/python3: No module named pytest

/tmp/t07-xhs-venv/bin/python -m pytest
40 passed, 1 warning in 6.30s
```

说明：系统 Python 为 3.9.6，不满足项目 `>=3.12,<3.13`；已使用 Codex bundled Python 3.12.13 创建临时 venv `/tmp/t07-xhs-venv` 并安装 `.[dev]` 后运行测试，未修改依赖清单。

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 可处理一条 `search` 采集任务，并调用 Adapter 搜索 | 是 | `test_search_task_collects_results_saves_snapshot_and_completes` |
| 搜索结果写入 `contents`，重复执行不产生重复内容 | 是 | `test_repeated_search_execution_is_idempotent_for_contents_and_discoveries` |
| 搜索结果写入 `discovery_relations`，同一 query/content 重复发现保持幂等 | 是 | `test_repeated_search_execution_is_idempotent_for_contents_and_discoveries` |
| 同一次搜索页保存一条可追溯原始快照记录，快照内容哈希稳定 | 是 | `test_search_task_collects_results_saves_snapshot_and_completes`、`test_snapshot_hash_is_stable_for_same_search_page` |
| 搜索分页游标保存到任务 `cursor_json` | 是 | 成功页和分页页测试均断言 `cursor_json` |
| 无后续页时任务进入 `completed`；有后续页时任务进入 `partial` 并保留游标 | 是 | `test_search_task_collects_results_saves_snapshot_and_completes`、`test_search_task_marks_partial_and_keeps_next_cursor` |
| Adapter 或入库异常时任务通过状态机进入 retry/failed，并记录错误 | 是 | `test_adapter_error_moves_task_to_retry_and_records_error` |
| 测试覆盖成功页、分页页、重复执行幂等、异常重试和快照写入 | 是 | `tests/test_xhs_search_collection.py` |
| 不修改数据库模型、迁移、查询 API、调度状态机和全局进度文件 | 是 | 修改文件均在 T07 允许范围内 |

## 未完成事项

- 未验证真实 Chrome 登录态；本任务按简报使用 `MockPlatformAdapter` 测试，不依赖真实小红书网络。

## 风险与问题

- 当前仅实现 T07 要求的搜索页采集与游标保存；`partial` 任务的完整断点续传调度策略不在本任务范围内。
- 未进行真实小红书页面验证，不能将 Mock 测试视为真实页面验证。

## 决策建议

- 无需主控决策。

## 建议的下一步

- 主控验收 T07 后，可进入详情采集或完整断点续传相关任务。
