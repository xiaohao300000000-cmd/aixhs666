# 子任务报告：T05

## 执行结果

- 状态：COMPLETED
- 分支：task/T05-dedup-discovery-relations
- 提交：未提交；等待主控验收。当前 HEAD：7886ce3
- 执行会话：W1

## 完成内容

- 新增存储层 ingest 服务，支持内容、评论、公开用户资料按平台 ID 去重写入。
- 新增搜索结果发现关系写入能力，支持多查询指向同一内容，并对同一 query/content 保持幂等。
- 新增稳定文本哈希工具，统一处理 Unicode、空白和大小写后生成 SHA-256。
- 新增测试覆盖 T05 验收条件。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `storage/ingest.py` | 新增存储层 ingest/dedup 服务和发现关系幂等写入 |
| `storage/text_hash.py` | 新增规范化文本哈希工具 |
| `storage/__init__.py` | 导出 T05 新增存储服务与文本哈希工具 |
| `tests/test_storage_ingest.py` | 覆盖内容、评论、用户资料、发现关系和文本哈希验收条件 |
| `orchestration/reports/T05.md` | 记录本任务执行结果 |

## 测试结果

```text
命令：pytest
结果：失败；当前 shell 中未安装 pytest（zsh: command not found: pytest）。

命令：python3 -m pytest
结果：失败；系统 Python 3.9 环境未安装 pytest。

命令：/tmp/t05-codex-venv312/bin/python -m pytest
结果：通过。30 passed, 1 warning in 7.03s。

说明：按简报允许方式在仓库外创建临时 venv，并使用 Codex 内置 Python 3.12.13 安装测试依赖后运行。
警告：FastAPI/Starlette TestClient 的第三方弃用警告，不影响本任务测试结果。
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 同一内容重复 ingest 后只保留一行，并更新可变字段和 `last_seen_at` | 是 | `tests/test_storage_ingest.py::test_content_ingest_deduplicates_and_updates_mutable_fields` |
| 同一评论重复 ingest 后只保留一行，并更新可变字段和 `last_seen_at` | 是 | `tests/test_storage_ingest.py::test_comment_ingest_deduplicates_and_updates_mutable_fields` |
| 同一公开用户重复 ingest 后只保留一行，并更新资料和 `last_seen_at` | 是 | `tests/test_storage_ingest.py::test_profile_ingest_deduplicates_and_updates_profile_fields` |
| 同一内容被两个不同查询发现时产生两条关系，但内容仍只有一行 | 是 | `tests/test_storage_ingest.py::test_multiple_queries_can_discover_one_content_without_content_duplication` |
| 同一 query/content 重复发现不重复写入关系 | 是 | `tests/test_storage_ingest.py::test_same_query_content_discovery_is_idempotent` |
| 文本哈希对空白、大小写等基础差异稳定，不同文本不同哈希 | 是 | `tests/test_storage_ingest.py::test_stable_text_hash_normalizes_basic_text_differences` |
| 测试使用现有 ORM 模型和 T04 Mock 数据，不依赖网络采集 | 是 | 新增测试使用 SQLite 内存库与 `MockPlatformAdapter` |
| 不修改数据库模型、迁移、采集器接口和全局进度文件 | 是 | 改动仅限 `storage/**`、`tests/**`、`orchestration/reports/T05.md` |

## 未完成事项

- 无。

## 风险与问题

- `discovery_relations` 未改数据库唯一约束；同一 query/content 的幂等由存储服务层查询保证，符合本任务“不修改数据库模型或迁移”的限制。
- 当前本机默认 Python/pytest 不可直接运行项目测试；已用仓库外临时 Python 3.12 venv 完成验证。

## 决策建议

- 暂无需要主控决定的事项。

## 建议的下一步

- 主控验收 T05 后，再安排后续任务接入该存储服务。
