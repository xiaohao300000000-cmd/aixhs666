# 子任务报告：V19-05

## 执行结果

- 状态：COMPLETED（真实发送门槛按要求未越过）
- 分支：`codex/v19-05-public-reply`
- worktree：`/Users/xiaohao30000/.codex/worktrees/f09f/aixhs666`
- 提交：截至本报告提交共 30 个实现、测试、修复与报告提交
- 执行会话：V19-05 独立执行对话

## 完成内容

- 增加 `0021` 迁移、草稿/批准 revision、排队时间和持久命令幂等事实；旧审批安全回填。
- 推进合格评论客户后幂等排队 `comment_reply_prepare`，外部生成器和飞书只由 Worker 调用。
- 分离编辑、确认话术、最终发送、平台结果和人工确认未发送命令；第一步绝不发送。
- 飞书回调改为两步协议，正式回调路径不构造 sender；第二步创建唯一持久发送任务。
- Worker 使用 task payload 的 `draft_revision` 与 `attempt_count` 双重 fencing，并通过 `record_contact_result` 唯一落库。
- 编辑只允许安全草稿态；queued、legacy approved_to_send、sending、result_unknown、sent、cancelled 均不可改变 revision。
- 不同幂等键发送命令由 PostgreSQL reply 行锁串行化，最多持久化一个 send task 和一个 queue timeline。
- 飞书 Card 2.0 使用 callback behaviors/form_action_type；最终 callback token 随唯一任务持久化并由 Worker 回写原卡。
- 删除旧 callback-sender 公开入口；正式与测试路径均以 Worker execute 作为唯一 sender 所有者。
- prepare 幂等重放动态读取持久 task 五态；失败只返回净化摘要，妙搭恢复“重新生成”并创建新 intent key，在途轮询继续复用原 key。
- sent/failed/result_unknown 同步客户核心事实；投影失败不回滚、不重发。
- 增加 Operator API、NestJS BFF、妙搭客户详情的同一版本/状态合同；私信保持未接入。
- 完成自动化、真实 PostgreSQL schema 升级、隔离 PostgreSQL 升降级保留验证、文档和双报告。

## 修改文件

| 分组 | 文件 |
|---|---|
| 模型/迁移 | `storage/models.py`；`alembic/versions/0021_contact_reply_two_step.py` |
| 核心服务 | `services/contact_commands.py`；`services/customer_progression.py`；`services/feishu_customer_followup.py`；`services/operator_customers.py` |
| 飞书/API/Worker | `integrations/feishu/comment_replies.py`；`apps/api/routes/feishu_callbacks.py`；`apps/api/routes/operator_api.py`；`apps/worker/comment_reply_prepare.py`；`apps/worker/comment_reply_send.py`；`apps/worker/main.py` |
| 妙搭 | `miaoda-console/server/modules/operator/operator.controller.ts`；`operator.service.ts`；`client/src/api/operator.ts`；`client/src/types/operator.ts`；`client/src/features/operator/operator-view-model.ts`；`client/src/pages/CustomerDetailPage.tsx` |
| 测试 | `tests/test_contact_commands.py`；`test_comment_reply_workflow.py`；`test_customer_crm_operator.py`；`test_customer_progression.py`；`test_lead_comment_reply_migration.py`；`test_operator_api.py`；`test_worker_runtime.py`；妙搭两个 unit spec |
| 文档/报告 | `docs/COMMENT_REPLY_OPERATIONS.md`；`docs/DATA_MODEL.md`；`docs/ARCHITECTURE.md`；`docs/reports/V19_05_PUBLIC_REPLY_VERIFICATION.md`；`orchestration/reports/V19-05.md` |

## 测试结果

```text
第二轮聚焦 Python 套件：96 passed, 5 skipped, 1 warning
G 受影响 Python：49 passed, 1 skipped, 1 warning
全量 pytest：629 passed, 9 skipped, 1 warning
妙搭 Jest：52 passed
妙搭 type:check：server/client passed
妙搭 lint：eslint/typecheck/stylelint passed
妙搭 build：completed
git diff --check：passed

真实 PostgreSQL：
0021_contact_reply_two_step / replies=0 / command_operations=0（本轮只读复查）

一次性隔离 PostgreSQL 并发：
不同 Miaoda/飞书 idempotency key → 1 queued + 1 state_conflict
→ 1 comment_reply_send task / 1 contact_send_queued timeline

一次性隔离 PostgreSQL：
0020 seeded legacy reply → 0021 preserved + revision backfilled
→ downgrade 0020 preserved legacy fields
→ upgrade 0021 preserved + revision backfilled again
→ disposable database dropped
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 至少九个有意义提交 | 是 | 截至本报告提交共 30 个提交 |
| 严格 TDD | 是 | 8 组显式 RED/GREEN；主控审计返工另有 RED/GREEN |
| 持久版本与幂等 | 是 | `0021`、`ContactCommandOperation`、命令测试 |
| 从无草稿的产品路径 | 是 | progression 只排队，prepare Worker 生成；真实库 0 条基线如实记录 |
| 两步确认 | 是 | approve 不建 task；send 要求 exact revision/text/confirmed |
| 唯一 sender | 是 | 旧 callback-sender API 与导出已删除；Worker execute 是唯一 sender 路径 |
| Card 2.0 与原卡回写 | 是 | callback behaviors 精确测试；task payload 固定 update_token；三结果均回写该 token |
| 并发唯一发送任务 | 是 | 真实 PostgreSQL 不同幂等键竞态只产生一个 task/event，另一路稳定 state_conflict |
| prepare 失败可恢复 | 是 | task 五态动态合同；失败摘要不含原始错误；按钮恢复且下一次使用新 intent key |
| 客户事实与投影隔离 | 是 | 三结果写命令；Base 投影失败后核心事实仍存在且 sender 一次 |
| unknown 安全恢复 | 是 | 禁止普通重试；仅人工确认未发送 |
| Operator/BFF/妙搭同合同 | 是 | API、BFF、类型、视图模型与页面测试/构建通过 |
| 真实 PostgreSQL 非破坏迁移 | 是 | 真实 0→0 快照；隔离库升降级保留夹具 |
| 未触发外部写入 | 是 | 无 XHS send、真实 Base write、真实飞书卡、发布或推送 |
| 文件范围 | 是 | 全部修改均在 V19-05 allowlist；全局进度文件未改 |

范围备注：`integrations/feishu/__init__.py` 原不在简报 allowlist。第二轮审查 E 要求删除公开直发旁路后，主控已正式批准仅扩展该文件中删除 `apply_comment_reply_callback` 导入与 `__all__` 导出两行；本分支没有对该文件做其他修改。

## 未完成事项

- 没有 V19-05 代码或自动化未完成项。
- 真实 selector probe 与单条发送未执行，因为五项批准门槛均未齐备；这属于正确安全停止，不是伪造验收。
- 真实 Base/飞书投影验收留给主控在配置、代码验收和用户逐条批准后执行。

## 风险与问题

- 真实库仍为 0 条回复；真实“生成草稿并建卡”需要主控另行安排受控验收窗口。
- Alembic 配置仍产生既有 `path_separator` deprecation warning；不影响本轮 upgrade/downgrade 执行。
- 既有 Starlette/httpx2 与 ts-jest warning 未阻塞测试。

## 决策建议

- 主控合并前重点复核行锁串行化、Worker 唯一 sender、task revision/token fencing、第二步发送文案和真实发送五项门槛。
- 若安排真实验收，先提供专用目标和账号/CDP，只运行不填写/不提交的 selector probe；通过后再取得用户对该目标与最终文本的逐条批准。

## 建议的下一步

- 主控验收本分支与两份报告；验收前不合并、不推送、不发布。
- 本执行对话不进入 V19-06，完成后停止等待主控。
