# 子任务报告：T20

## 执行结果

- 状态：COMPLETED
- 分支：task/T20-data-dashboard
- 提交：本报告所在提交
- 执行会话：T20 子 CLI 卡住后由主控接手完成

## 完成内容

- 新增 `intelligence.dashboard` 聚合层，支持每日新增、重复率、查询产出、来源评分、新词审核、失败任务和字段完整度汇总。
- 新增 worker 调用入口 `build_worker_dashboard_summary`。
- 新增 `/dashboard/summary` FastAPI 路由，接受本地指标 payload 并返回看板摘要。
- 新增 T20 测试覆盖核心比率、排行、非法计数校验和 API 路由。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `intelligence/dashboard/__init__.py` | 导出看板公共接口 |
| `intelligence/dashboard/summary.py` | 实现看板输入、摘要对象、聚合与校验逻辑 |
| `apps/worker/data_dashboard.py` | 提供 worker 可调用入口 |
| `apps/api/routes/dashboard.py` | 提供本地可测试看板摘要 API |
| `apps/api/main.py` | 挂载 dashboard 路由 |
| `tests/test_data_dashboard.py` | 覆盖 T20 验收条件 |
| `orchestration/reports/T20.md` | 创建子任务报告 |

## 测试结果

```text
/tmp/ai-hawke-xhs-main-venv/bin/python -m pytest tests/test_data_dashboard.py -q
4 passed, 1 warning in 0.25s

/tmp/ai-hawke-xhs-main-venv/bin/python -m pytest -q
110 passed, 1 warning in 0.58s
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 可从批量指标输入生成看板摘要 | 是 | `build_dashboard_summary` 与 `test_dashboard_summary_calculates_core_rates_and_totals` |
| 可计算重复率、字段完整度、失败率和查询产出排行 | 是 | `test_dashboard_ranks_queries_sources_failed_tasks_and_field_gaps` |
| 可暴露本地可测试 API 或 worker 调用入口 | 是 | `build_worker_dashboard_summary` 与 `/dashboard/summary` 测试 |
| 不新增依赖，不修改数据库模型、迁移、全局进度文件 | 是 | 修改范围限 T20 简报允许文件 |
| `pytest` 通过 | 是 | 全量 `110 passed, 1 warning` |

## 未完成事项

- 无。

## 风险与问题

- 当前 API 接收调用方提供的指标 payload，不直接查询数据库；后续如需真实看板服务，需要接入 repository 聚合。

## 决策建议

- 暂无需要主控决策事项。

## 建议的下一步

- 主控验收并合并 T20 后，可派发 T21 内容洞察输出。
