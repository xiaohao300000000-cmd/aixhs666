# 子任务报告：T09

## 执行结果

- 状态：COMPLETED
- 分支：`task/T09-xhs-comment-collection`
- 提交：`d248876d5bd95066ca825826f4ed0f6e837e7039`（报告写入前的实现提交；最终 HEAD 哈希见交付消息）
- 执行会话：W1 / T09 独立子 CLI

## 完成内容

- 新增评论采集 worker 入口，支持 `comments`、`collect_comments`、`comment_collection` 任务类型。
- 评论任务从 `target_id`、`payload_json.platform_content_id` 或已有游标读取平台内容 ID，调用 `PlatformAdapter.list_comments(platform_content_id, cursor, limit)`。
- 将一级评论和同页二级回复写入 L2 comments，自动建立 `parent_comment_id`，并通过现有 ingest 逻辑创建评论者 `public_profiles` 身份记录。
- 保存 content 级 `comments_page` JSON 快照，并将 `next_cursor`、`has_more`、`limit`、`platform_content_id` 写入 `CollectionTask.cursor_json`。
- 无后续页标记 `completed`，有后续页标记 `partial`，异常调用 `fail_task` 后继续抛出。
- 新增 Mock 测试覆盖成功页、分页页、幂等、回复关系、异常重试、快照写入和 payload 目标 ID。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `apps/worker/comment_collection.py` | 新增 T09 评论采集任务执行入口、游标处理、快照保存和状态转换 |
| `tests/test_xhs_comment_collection.py` | 新增 T09 评论采集验收测试 |
| `orchestration/reports/T09.md` | 创建本子任务报告 |

## 测试结果

```text
$ /tmp/ai-hawke-xhs-t09-venv/bin/python -m pytest tests/test_xhs_comment_collection.py
7 passed in 6.08s

$ /tmp/ai-hawke-xhs-t09-venv/bin/python -m pytest
52 passed, 1 warning in 0.50s
```

说明：系统无全局 `pytest` 命令，按任务要求使用 `/Users/xiaohao30000/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3` 创建 `/tmp/ai-hawke-xhs-t09-venv`，并执行 `pip install -e '.[dev]'` 后测试。未提交 venv、锁文件或依赖清单变更。

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 可处理一条评论采集任务，并调用 Adapter 获取评论页 | 是 | `run_next_comment_task` / `run_comment_task` 调用 `adapter.list_comments(...)`；测试 `test_comment_task_collects_l2_comments_profiles_snapshot_and_completes` |
| 一级评论写入 `comments`，重复执行不产生重复评论 | 是 | `ingest_comment` 幂等复用；测试 `test_repeated_comment_execution_is_idempotent_for_comments_and_profiles` |
| 二级回复通过 `parent_comment_id` 建立父子关系 | 是 | `_parent_first` 保证同页父评论优先；测试 `test_comment_task_builds_reply_relationship_when_reply_precedes_parent` |
| 评论者公开 ID 映射或创建 `public_profiles` | 是 | `ingest_comment` 创建 profile identity；成功页和幂等测试断言评论者 profile |
| 评论分页游标保存到 `CollectionTask.cursor_json` | 是 | `_cursor_payload` 包含 `next_cursor`、`has_more`、`limit`、`platform_content_id`；分页测试断言 |
| 同一次评论页保存可追溯原始快照，快照内容哈希稳定 | 是 | `save_json_snapshot(... snapshot_type="comments_page")`；成功页断言文件存在，幂等测试断言哈希稳定 |
| 无后续页进入 `completed`，有后续页进入 `partial` | 是 | 成功页测试断言 `completed`，分页测试断言 `partial` |
| Adapter 或入库异常时进入 retry/failed，并记录错误 | 是 | `except` 中调用 `fail_task` 后抛出；测试 `test_comment_adapter_error_moves_task_to_retry_and_records_error` |
| 测试覆盖成功页、分页页、幂等、回复关系、异常重试和快照写入 | 是 | `tests/test_xhs_comment_collection.py` 共 7 项 |
| 不修改数据库模型、迁移、查询 API、调度状态机和全局进度文件 | 是 | 修改文件仅限允许范围 |

## 未完成事项

- 未验证真实小红书 Chrome 登录态；本任务按要求使用 MockPlatformAdapter 测试，不依赖真实网络。

## 风险与问题

- 当前仅覆盖 Adapter 已返回同一 `CommentPage` 中父子关系的场景；跨页回复依赖父评论已在前序页入库。
- 真实小红书页面结构、登录态、验证码和限频风险未在本任务验证。
- 全量测试存在 1 条第三方弃用警告：`StarletteDeprecationWarning`，不影响当前测试结果。

## 决策建议

- 无需要主控新增决策的事项。

## 建议的下一步

- 主控验收 T09 后，可按依赖继续安排 T10 断点续传与部分成功任务。
