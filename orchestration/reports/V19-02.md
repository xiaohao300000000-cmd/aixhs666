# 子任务报告：V19-02

## 执行结果

- 状态：COMPLETED
- 分支：`codex/v19-02-base-crm`
- 提交：`5613f2c`、`095e92c`、`c92a849`、`0de5ede`、`5379749`、`d1ddd28`、`1908a94`、`21f5959`，以及报告提交
- 执行会话：V19-02 独立执行对话

## 完成内容

- 增加 Lead CRM 事实字段、`0018_customer_crm` 迁移和一事一行的 `CustomerFollowupRecord`。
- `promote` 幂等创建首次跟进事实并返回 CRM 同步待处理结果，事务内不调用飞书网络。
- 实现客户/跟进 Base 投影、持久映射、精确键 reconciliation、失败隔离、白名单 pull、版本和远端时间冲突保护。
- 实现受 token 保护的客户列表、详情、时间线和同步 Operator API，以及 Base/妙搭深链。
- 真实 PostgreSQL 已迁移到 `0018_customer_crm (head)`。
- 真实 Base 已非破坏扩展客户表，并新增 `跟进记录` 表和 CRM 视图。
- 真实客户 147 完成重复 upsert、`跟进备注` 可逆回写和 PostgreSQL/Base/API 三端一致性验收。
- 统一规格 CRM 阶段已对齐；deferred candidate 不会进入客户 CRM，空 record ID 创建结果进入 reconciliation。
- 真实跟进事实 4 连续同步两次，Base 精确键始终一行并复用持久映射。
- 正式客户进入 `invalid/无效` 后仍在 CRM 中可见、可同步；非正式候选继续隔离。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `storage/models.py` | 增加 CRM 字段和跟进记录事实模型 |
| `alembic/versions/0018_customer_crm.py` | 新字段、回填、跟进事实表和索引迁移 |
| `services/customer_progression.py` | 推进客户后幂等创建首次跟进事实 |
| `services/customer_crm_sync.py` | 客户/跟进投影、白名单 pull、冲突与 reconciliation |
| `services/operator_customers.py` | 客户列表、详情、时间线和深链数据 |
| `integrations/feishu/bitable.py` | CRM 表配置与 lark-cli 单记录更新时间读取 |
| `apps/api/routes/operator_api.py` | 客户资源 API 和幂等同步入口 |
| `.env.example` | CRM/跟进表和妙搭深链配置示例 |
| `tests/test_customer_crm_models.py` | CRM 模型和迁移结构测试 |
| `tests/test_customer_crm_sync.py` | 投影、幂等、reconciliation、白名单与真实 schema 兼容测试 |
| `tests/test_customer_crm_operator.py` | 客户服务、深链和迁移客户可见性测试 |
| `tests/test_customer_progression.py` | 首次跟进事实和返回后果测试 |
| `tests/test_operator_api.py` | 客户 API、认证和同步幂等键测试 |
| `tests/test_operator_gateway.py` | 公网网关路径边界测试 |
| `docs/reports/V19_02_BASE_CRM_VERIFICATION.md` | 完整自动化与真实环境验收证据 |
| `orchestration/reports/V19-02.md` | 子任务交付报告 |

## 测试结果

```text
/Users/xiaohao30000/aixhs666/.venv/bin/pytest tests/test_customer_crm_models.py tests/test_customer_crm_sync.py tests/test_feishu_customer_followup.py tests/test_customer_progression.py tests/test_operator_api.py tests/test_operator_gateway.py -q
61 passed, 1 warning in 1.12s

/Users/xiaohao30000/aixhs666/.venv/bin/pytest -q
557 passed, 7 skipped, 1 warning in 26.42s

/Users/xiaohao30000/aixhs666/.venv/bin/alembic current
0018_customer_crm (head)

git diff --check
通过
```

warning 为既有 FastAPI TestClient/httpx2 弃用提示。

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 自动化全部通过 | 是 | 指定集 61 passed；全量 557 passed、7 skipped |
| 真实 PostgreSQL migration | 是 | `0017` 成功升级到 `0018_customer_crm (head)` |
| 真实 Base 非破坏变更 | 是 | 既有客户/候选/证据/控制记录未删改；仅新增字段、表、视图和 1 条真实客户投影 |
| 真实客户幂等 upsert | 是 | 客户 147 连续同步两次；Base 精确键始终 1 行，record `recvpxkvVZRcv6` |
| 白名单可逆回写 | 是 | `跟进备注` null→临时值→null，版本 0→1→2 |
| PostgreSQL/Base/API 一致 | 是 | 最终 ID 147、阶段 new_customer/新客户、版本 2、备注 null、同步 synced |
| 不重复客户/跟进 | 是 | 客户精确键 1 行；跟进 ID 4 精确键 1 行，record `recvpxpCIVE6CC` |
| 未触发采集或外发 | 是 | 未调用采集、公开回复、私信、回复检查或调度路径 |

## 未完成事项

- 无 V19-02 必须项未完成。
- 未实现 V19-03 以后内容；未发布妙搭客户中心。

## 风险与问题

- 为保护既有集成，物理表名仍为 `客户跟进表`，新增 `客户 CRM` 视图表达新用途。
- lark-cli pull 需对待处理客户逐条读取 record history 才能获得远端更新时间，大批量人工编辑时会增加延迟。
- 请求要求幂等键，但请求级幂等键未独立持久化；实体写入由精确键与映射幂等。
- 未配置妙搭公网 URL 时深链为相对路径，完整页面和发布属于 V19-04。
- 为兼容旧 Base 值，CRM 阶段字段保留旧选项；后端只接受统一规格的正式阶段。

## 决策建议

- 主控验收时确认是否接受“保留物理表名 + `客户 CRM` 视图”的兼容策略。
- 后续如 Base 人工编辑规模明显增加，可在独立任务评估服务端增量查询或历史批量能力，不应在本任务扩展。
- 后续可决定是否为同步 API 增加请求级幂等账本；当前实体级幂等已满足重复同步不重复写行。

## 建议的下一步

- 主控检查本分支提交、两份报告与真实 Base 状态后完成 V19-02 验收。
- 验收完成前不要开始 V19-03；本执行对话停止等待主控。
