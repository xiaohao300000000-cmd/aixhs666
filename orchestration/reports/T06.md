# 子任务报告：T06

## 执行结果

- 状态：COMPLETED
- 分支：task/T06-query-management-api
- 提交：未提交；按子任务要求完成后停止等待主控验收。当前基线提交为 `99f5eb4ac11531945154cad6c10ef736306ae77a`
- 执行会话：W1

## 完成内容

- 新增查询管理 API，支持查询创建、列表过滤、详情读取、局部更新。
- 新增查询启用、暂停、手动运行和基础统计接口。
- 新增查询 API 测试，使用 SQLite 内存库和 FastAPI dependency override，不依赖真实 PostgreSQL 或网络。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `apps/api/main.py` | 挂载查询管理 router。 |
| `apps/api/routes/queries.py` | 实现 T06 查询 CRUD、启停、手动运行和统计 API。 |
| `tests/test_query_api.py` | 覆盖查询 API 验收场景。 |
| `orchestration/reports/T06.md` | 记录本子任务执行结果。 |

## 测试结果

```text
pytest
zsh:1: command not found: pytest

/tmp/ai-hawke-xhs-t06-venv/bin/pytest
collected 35 items
tests/test_core_data_models.py .....                                     [ 14%]
tests/test_health.py .                                                   [ 17%]
tests/test_mock_platform_adapter.py ......                               [ 34%]
tests/test_query_api.py .....                                            [ 48%]
tests/test_storage_ingest.py ........                                    [ 71%]
tests/test_task_state_machine.py ..........                              [100%]

35 passed, 1 warning in 0.38s
```

说明：系统 Python 为 3.9 且无 `pytest`，项目要求 Python 3.12；因此使用 `/tmp/ai-hawke-xhs-t06-venv` 临时 venv 安装项目 dev 依赖后运行测试，未提交任何环境文件。

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 可以创建查询，默认状态为 `active`，默认优先级为 `0`。 | 是 | `POST /queries`；`tests/test_query_api.py::test_create_query_defaults_and_read_detail`。 |
| 可以列表查询，并按平台、状态和查询类型过滤。 | 是 | `GET /queries` 支持 `platform`、`status`、`query_type`；`test_list_queries_filters_by_platform_status_and_type`。 |
| 可以读取单个查询，不存在时返回 404。 | 是 | `GET /queries/{query_id}`；`test_create_query_defaults_and_read_detail`。 |
| 可以更新查询文本、状态和优先级，并持久化。 | 是 | `PATCH /queries/{query_id}`；`test_update_query_and_start_stop`。 |
| start/stop 能在 `active` 和 `paused` 之间切换状态。 | 是 | `POST /queries/{query_id}/start` 和 `/stop`；`test_update_query_and_start_stop`。 |
| 手动运行能创建 `search` 类型、绑定原查询、可领取的 `pending` 采集任务。 | 是 | `POST /queries/{query_id}/run` 调用 `scheduler.create_task`；`test_manual_run_creates_pending_search_task`。 |
| 查询统计接口返回基础统计字段。 | 是 | `GET /queries/{query_id}/stats` 返回 `run_count`、`last_run_at`、`next_run_at`、`discovery_count`、`task_count`；`test_query_stats_returns_basic_counts`。 |
| 测试使用 SQLite 内存库或临时数据库，不依赖真实 PostgreSQL 或网络。 | 是 | `tests/test_query_api.py` 使用 SQLite in-memory + `StaticPool`。 |
| 不修改数据库模型、迁移、调度状态机、采集器接口和全局进度文件。 | 是 | 仅修改允许范围内文件；未改 `storage/**`、`scheduler/**`、`alembic/**` 或全局进度文件。 |

## 未完成事项

- 无。

## 风险与问题

- 测试环境出现 `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated; install httpx2 instead.`，不影响本任务测试结果。

## 决策建议

- 无需主控额外决策。

## 建议的下一步

- 主控验收 T06，并决定是否提交、合并或进入后续任务。
