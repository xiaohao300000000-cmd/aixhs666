# 子任务报告：T04

## 执行结果

- 状态：COMPLETED
- 分支：task/T04-platform-adapter-mock
- 提交：0116dd2；本子任务保持工作区未提交，等待主控验收后统一处理。
- 执行会话：W1

## 完成内容

- 新增 `PlatformAdapter` 协议，定义搜索、内容详情、评论列表、公开用户资料读取边界。
- 新增统一采集数据对象，覆盖搜索结果、内容详情、评论、用户资料、分页游标。
- 新增确定性 `MockPlatformAdapter`，提供查询、内容、评论、用户 Mock 数据。
- 新增测试覆盖 Mock 边界读取、父子评论、用户资料和 ORM 一次性提交映射。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `collectors/__init__.py` | 导出采集协议、统一对象和 Mock Adapter。 |
| `collectors/base.py` | 定义 PlatformAdapter 协议和统一采集数据对象。 |
| `collectors/mock.py` | 实现确定性内存 Mock Adapter。 |
| `tests/test_mock_platform_adapter.py` | 覆盖 T04 Mock 行为和 ORM 映射验收。 |
| `orchestration/reports/T04.md` | 创建本子任务报告。 |

## 测试结果

```text
pytest
结果：失败，当前 shell 中未安装 pytest 命令。

python3 -m pytest
结果：失败，系统 python3 为 3.9.6 且未安装 pytest；项目要求 Python >=3.12,<3.13。

/tmp/aihawke-t04-venv/bin/pytest
结果：通过。22 passed, 1 warning in 0.30s。
说明：按简报允许方式使用仓库外临时 venv，Python 3.12.13，安装的是 pyproject.toml 声明的运行与 dev 依赖。
警告：FastAPI/TestClient 依赖链产生 StarletteDeprecationWarning，与本任务改动无关。
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| PlatformAdapter 定义搜索、内容详情、评论和用户资料边界 | 是 | `collectors/base.py` 中 `PlatformAdapter.search/get_content/list_comments/get_profile`。 |
| 统一对象包含平台侧内容、评论、用户、正文、标题、互动、发布时间、URL、区域等字段 | 是 | `CollectedSearchResult`、`CollectedContent`、`CollectedComment`、`CollectedProfile`。 |
| Mock Adapter 输出确定、可重复且不依赖网络 | 是 | `collectors/mock.py` 使用固定内存数据；测试断言重复搜索结果一致。 |
| 测试覆盖查询、详情、评论、用户资料 | 是 | `tests/test_mock_platform_adapter.py` 中 5 个行为测试。 |
| Mock 数据可映射到 Query、Content、Comment、PublicProfile 并提交 | 是 | `test_mock_data_maps_to_core_orm_models` 使用 SQLite 内存库提交成功。 |
| 不实现 T05 去重、文本哈希、upsert 或发现关系逻辑 | 是 | 仅一次性插入测试数据，未新增去重服务、哈希或发现关系写入。 |
| 不修改数据库模型、迁移和全局进度文件 | 是 | 改动仅限 `collectors/**`、`tests/**`、本报告。 |

## 未完成事项

- 无。

## 风险与问题

- 本地全局环境没有可直接使用的 `pytest`，系统 `python3` 版本也低于项目要求；已使用仓库外临时 venv 完成验证。

## 决策建议

- 无需主控额外决策。

## 建议的下一步

- 主控验收 T04 后，再由后续任务接入去重、发现关系或真实平台采集。
