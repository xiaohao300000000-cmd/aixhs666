# 子任务报告：T17

## 执行结果

- 状态：COMPLETED
- 分支：task/T17-event-calendar
- 提交：见最终交付哈希
- 执行会话：T17 独立子 CLI

## 完成内容

- 新增教育事件对象、事件类型、事件状态和查询输入对象。
- 实现事件状态计算：upcoming、active、cooldown、expired。
- 实现查询优先级调整纯函数，输出 boost、reason 和 valid_until。
- 新增 worker 可调用入口，接收事件列表和查询列表并返回优先级建议。
- 增加事件日历单元测试，覆盖窗口状态、boost 排序、过期事件过滤、worker 入口和输入校验。

## 修改文件

| 文件 | 修改原因 |
|---|---|
| `intelligence/event_calendar/__init__.py` | 导出事件日历公共接口 |
| `intelligence/event_calendar/calendar.py` | 实现事件对象、状态计算和优先级建议纯函数 |
| `apps/worker/event_calendar.py` | 提供 worker 可调用薄入口 |
| `tests/test_event_calendar.py` | 覆盖 T17 验收条件 |
| `orchestration/reports/T17.md` | 子任务交付报告 |

## 测试结果

```text
.venv/bin/pytest tests/test_event_calendar.py
7 passed in 0.01s

.venv/bin/pytest
97 passed, 1 warning in 6.42s
```

## 验收条件自检

| 条件 | 是否满足 | 证据 |
|---|---|---|
| 可定义教育事件及时间窗口 | 是 | `EducationEvent` 包含名称、类型、地域、开始/结束时间、预热/冷却窗口、关联查询词 |
| 可按当前时间计算事件状态 | 是 | `get_event_status` 和 `test_event_status_uses_warmup_active_cooldown_and_expired_windows` |
| 可为相关查询生成优先级调整建议 | 是 | `generate_event_priority_suggestions` 输出 `EventPrioritySuggestion` |
| 临近/进行中事件的 boost 高于过期事件 | 是 | 过期事件不生成建议，active boost 高于 cooldown |
| 不新增依赖，不修改数据库模型、迁移、全局进度文件 | 是 | 仅修改 T17 允许范围文件 |
| `pytest` 通过 | 是 | 全量 `97 passed, 1 warning` |

## 未完成事项

- 无。

## 风险与问题

- 当前为本地纯函数能力，尚未接入真实调度器；按简报要求本任务不接入调度器。

## 决策建议

- 无需主控决策。

## 建议的下一步

- 主控验收 T17 后，可继续派发依赖 T17 的 T19 信号新鲜度与飞书预警。
