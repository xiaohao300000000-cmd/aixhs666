# 子任务简报：T16

## 任务名称

飞书新词审核。

## 任务目标

在 T15 候选表达生成后，实现飞书新词审核的本地可测试边界：候选词推送 payload、批准、拒绝、转查询和状态回写对象。

本任务不调用真实飞书网络，不新增依赖，不修改数据库模型。

## 依赖

- 已完成任务：T15
- 必须阅读：
  - `WORKER_CODEX_PROMPT.md`
  - `orchestration/briefs/T16.md`
  - `templates/SUBTASK_REPORT.md`
  - `TASKS.md` 中 T16
  - `docs/ACCEPTANCE_TESTS.md` 中新词发现和飞书

## 分支与工作区

- 分支：`task/T16-feishu-phrase-review`
- Worktree：`../worktrees/T16`

## 允许修改

- `integrations/feishu/**`
- `apps/worker/**`
- `tests/test_feishu_phrase_review.py`
- `orchestration/reports/T16.md`

## 禁止修改

- `storage/models.py`
- `alembic/**`
- `scheduler/**`
- `apps/api/**`
- `docs/**`
- `TASKS.md`
- `PROJECT_DASHBOARD.md`
- `HANDOFF.md`
- `DECISIONS.md`
- `pyproject.toml`
- 其他未列入允许范围的文件

唯一例外：必须创建 `orchestration/reports/T16.md`。

## 输出

- 飞书新词审核卡片/payload 构造函数，不发真实网络。
- 审核动作对象：approve、reject、convert_to_query。
- 审核状态转换：pending、approved、rejected、converted_to_query。
- 将 approved/converted 候选转成查询创建请求的纯函数。
- worker 可调用入口，接受候选表达列表并返回待推送 payload。
- `orchestration/reports/T16.md`。

## 验收条件

1. 可为候选词构造飞书审核 payload。
2. 可批准、拒绝候选词。
3. 批准候选可转换为查询请求。
4. 状态转换可测试且非法转换报错。
5. 不调用真实飞书网络，不新增依赖，不修改数据库模型、迁移、全局进度文件。
6. `pytest` 通过。

## 提交要求

- 提交信息：`feat(T16): add feishu phrase review`
- 报告：`orchestration/reports/T16.md`
